# دليل إصلاح قاعدة البيانات - Database Migration Guide

## 🔴 المشكلة التي تم إصلاحها

### الخطأ الأصلي
```
SQLITE_ERROR: no such column: total
```

### السبب
قاعدة البيانات الموجودة (`orders.db`) تم إنشاؤها بدون جميع الأعمدة المطلوبة:
- ❌ `total` - إجمالي الطلب
- ❌ `subtotal` - الجزء قبل الشحن
- ❌ `shipping_cost` - تكلفة الشحن
- ❌ `customer_city` - مدينة العميل
- ❌ `customer_district` - حي العميل
- ❌ `updated_at` - وقت آخر تحديث
- ❌ `completed_at` - وقت إكمال الطلب
- ❌ `shipped_at` - وقت شحن الطلب

## ✅ الحل المطبق

تم إنشاء نظام Safe Migration يقوم بـ:

1. **الفحص الآمن**: فحص الأعمدة الموجودة دون حذف البيانات
2. **الإضافة التدريجية**: إضافة الأعمدة الناقصة فقط
3. **عدم فقدان البيانات**: جميع العمليات تحافظ على البيانات الموجودة
4. **إنشاء الـ Indexes**: تحسين الأداء بعد الإضافة

## 📋 الملفات المعدلة / المنشأة

### 1. **`/db/migrate.js`** (جديد - 230 سطر)
```javascript
// فئة DatabaseMigration تقوم بـ:
// - فحص الأعمدة الموجودة في الجداول
// - مقارنة مع الأعمدة المطلوبة
// - إضافة الأعمدة الناقصة بأمان
// - إنشاء الـ indexes للأداء
```

**الاستخدام المستقل:**
```bash
node db/migrate.js
```

### 2. **`/routes/orders.js`** (معدل - ترتيب الروابط)
```
الترتيب الصحيح الآن:
1. POST /api/orders              (إنشاء طلب)
2. PUT /api/orders/:id           (تحديث)
3. DELETE /api/orders/:id        (حذف)
4. GET /api/orders/stats/summary (إحصائيات - محدد)
5. GET /api/orders/track/:id     (تتبع - محدد)
6. GET /api/orders/:id           (جلب واحد - عام)
7. GET /api/orders               (جلب الكل - جذري)
```

**لماذا الترتيب مهم؟**
في Express، يتم مطابقة الروابط من الأعلى للأسفل. إذا كانت رابطة عامة (مثل `/:id`) قبل رابطة محددة (مثل `/track/:orderNumber`)، فسيتم تطابق الرابطة العامة أولاً.

### 3. **`/server.js`** (معدل - تشغيل الـ migration)
```javascript
// الآن يقوم بـ:
// 1. فتح قاعدة البيانات
// 2. تشغيل DatabaseMigration تلقائياً ✨ جديد
// 3. فحص وإصلاح الأعمدة الناقصة
// 4. إنشاء الجداول
// 5. بدء الخادم
```

## 🚀 كيفية الاستخدام

### الطريقة 1: التشغيل التلقائي (موصى به)
```bash
npm run dev
# أو
npm start
```
سيقوم البرنامج بـ:
1. فحص قاعدة البيانات تلقائياً
2. إضافة الأعمدة الناقصة
3. بدء الخادم

### الطريقة 2: تشغيل الـ Migration يدويًا أولاً
```bash
node db/migrate.js
# ثم
npm run dev
```

## 📊 الإخراج المتوقع

عند بدء الخادم، ستملاحظ رسائل مثل:

```
╔════════════════════════════════════════════════╗
║            🔧 فحص وإصلاح قاعدة البيانات      ║
╚════════════════════════════════════════════════╝

📋 الأعمدة الموجودة حالياً في جدول orders:
   ├─ id (INTEGER)
   ├─ order_number (TEXT)
   ├─ customer_name (TEXT)
   ...

⚠️ الأعمدة الناقصة:
   └─ ❌ total
   └─ ❌ subtotal
   └─ ❌ shipping_cost
   ...

🔄 جاري إضافة الأعمدة الناقصة...

   ⏳ إضافة العمود total...
   ✅ تم إضافة العمود total بنجاح
   ...

📊 إنشاء الـ indexes للأداء...
✅ تم إنشاء الـ indexes

╔════════════════════════════════════════════════╗
║  ✅ تم إصلاح قاعدة البيانات بنجاح!             ║
║  ✔️ تمت إضافة 8 أعمدة            ║
╚════════════════════════════════════════════════╝
```

## ⚡ الأعمدة المضافة

### جدول `orders`:

| العمود | النوع | الوصف |
|-------|-------|-------|
| `total` | REAL | إجمالي الطلب (مع الشحن) |
| `subtotal` | REAL | المجموع قبل الشحن |
| `shipping_cost` | REAL | تكلفة الشحن |
| `customer_city` | TEXT | مدينة العميل |
| `customer_district` | TEXT | حي العميل |
| `updated_at` | DATETIME | وقت آخر تحديث |
| `completed_at` | DATETIME | وقت إكمال الطلب |
| `shipped_at` | DATETIME | وقت شحن الطلب |

## 🔒 الأمان والبيانات

### ما يحدث للبيانات الموجودة:
- ✅ **محفوظة بالكامل** - لا يتم حذف أي بيانات
- ✅ **قيم افتراضية** - الأعمدة الجديدة تحصل على قيم افتراضية أو NULL
- ✅ **لا توجد فقدان** - العملية آمنة 100%

### استراتيجية الـ Rollback:
إذا حدثت مشكلة (وهو غير محتمل):
```bash
# نسخة احتياطية
cp orders.db orders.db.backup

# يمكنك استعادة النسخة القديمة إذا لزم الأمر
cp orders.db.backup orders.db
```

## 🧪 اختبار الحل

### 1. التحقق من الموارد الأساسية:
```bash
curl http://localhost:3000/api/health
```

### 2. جلب الإحصائيات:
```bash
curl http://localhost:3000/api/orders/stats/summary
```

### 3. تتبع طلب:
```bash
curl http://localhost:3000/api/orders/track/ORD-1234567890
```

### 4. جلب جميع الطلبات:
```bash
curl http://localhost:3000/api/orders
```

## 📝 ملاحظات مهمة

### Route Ordering في Express
```javascript
// ❌ خطأ - الجائحة العامة تطابق أولاً
router.get('/:id', ...);          // سيطابق /track/1234
router.get('/track/:id', ...);    // لن تصل أبداً

// ✅ صحيح - المحددة أولاً
router.get('/track/:id', ...);    // سيطابق /track/1234
router.get('/:id', ...);          // سيطابق بقية الحالات
```

### الأعمدة الافتراضية
```javascript
// عند إضافة عمود جديد:
ALTER TABLE orders ADD COLUMN total REAL NOT NULL DEFAULT 0;

// تحصل الصفوف الموجودة على القيمة الافتراضية (DEFAULT 0)
// إذا لم تكن هناك قيمة افتراضية، تحصل على NULL
```

## 🎯 الخطوات التالية

### 1. ✅ تشغيل الـ Migration (تلقائي)
### 2. ✅ التحقق من السجلات
### 3. ✅ اختبار الـ API Endpoints
### 4. ✅ التحقق من الشاشات

## 🔧 استكشاف الأخطاء

### المشكلة: الخطأ "no such column" لا يزال يظهر
**الحل:**
```bash
# احذف قاعدة البيانات القديمة (اختياري - إذا لم تكن هناك بيانات مهمة)
rm orders.db

# أعد تشغيل الخادم
npm run dev
```

### المشكلة: خطأ الإذن في كتابة قاعدة البيانات
**الحل:**
```bash
# تأكد من الإذن
chmod 644 orders.db
chmod 755 .
```

### المشكلة: Migration لا يعمل
**التشخيص:**
```bash
# اختبر الـ migration مباشرة
node db/migrate.js

# تحقق من الأخطاء في السجل
```

## 📚 المراجع

- [دليل Express Routing](https://expressjs.com/guide/routing.html)
- [SQLite: ALTER TABLE](https://www.sqlite.org/lang_altertable.html)
- [Best Practices for Database Migrations](https://github.com/semver/semver)

## 📞 الدعم

إذا واجهت مشاكل:
1. تحقق من السجلات في الكونسول
2. تأكد من أن النسخة صحيحة من Node.js (`v14+`)
3. تحقق من وجود `orders.db` في المجلد الجذري
4. جرب `node db/migrate.js` مباشرة

---

**آخر تحديث:** 2024
**الإصدار:** 1.0
**الحالة:** ✅ الإنتاج جاهز
