# دليل البدء السريع - نظام إدارة الطلبات

## تشغيل النظام

1. **تشغيل الخادم:**
```bash
npm run dev
```

2. **الوصول للواجهات:**
- الموقع الرئيسي: http://localhost:3001
- لوحة الإدارة الأساسية: http://localhost:3001/admin  
- لوحة الإدارة المتقدمة: http://localhost:3001/admin-enhanced
- تتبع الطلبات (للعملاء): http://localhost:3001/track

## استخدام النظام

### للعملاء:
1. تصفح المنتجات على الموقع الرئيسي
2. إضافة المنتجات للسلة
3. إتمام الطلب مع إدخال البيانات
4. تتبع الطلب باستخدام رقم الطلب

### للإدارة:
1. دخول لوحة الإدارة
2. عرض جميع الطلبات والإحصائيات
3. تحديث حالة الطلبات
4. تصدير التقارير بصيغة CSV

## ميزات النظام

✅ **نظام طلبات مكتمل** - بدون شوبيفاي
✅ **قاعدة بيانات SQLite محلية** - تحكم كامل
✅ **واجهتين إدارة** - أساسية ومتقدمة  
✅ **تتبع الطلبات للعملاء**
✅ **إحصائيات مفصلة**
✅ **تصدير CSV**
✅ **واجهة عربية RTL**
✅ **تصميم احترافي**

## حالات الطلبات

- `pending` - قيد المراجعة
- `confirmed` - مؤكد  
- `processing` - قيد التحضير
- `shipped` - تم الشحن
- `delivered` - تم التسليم
- `cancelled` - ملغي

## API Endpoints

- `POST /api/orders/create` - إنشاء طلب جديد
- `GET /api/orders` - جلب جميع الطلبات مع الإحصائيات
- `GET /api/orders/[id]` - جلب طلب محدد
- `PUT /api/orders/[id]` - تحديث حالة الطلب
- `GET /api/orders/track/[orderNumber]` - تتبع طلب بالرقم

## الملفات الهامة

- `lib/database.js` - قاعدة البيانات
- `pages/api/orders/` - API endpoints
- `app/admin/` - لوحات الإدارة
- `app/track/` - نظام التتبع
- `SYSTEM_DOCUMENTATION.md` - التوثيق الشامل

## الدعم والصيانة

النظام يعمل بشكل مستقل تماماً بدون اعتماد على خدمات خارجية.
جميع البيانات محفوظة محلياً في قاعدة بيانات SQLite.

**النظام جاهز للاستخدام الفوري! 🚀**